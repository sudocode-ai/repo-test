{
    "name": "poetry_subproject",

    "build": {
        "dockerfile": "Dockerfile",
        "context": "../.."
    },


    "customizations": {
        "kernels": [
            {
                "env_name": "poetry_subproject",
                "container_path": "/home/jovyan/.local/share/jupyter/kernels/poetry_subproject/kernel.json",
                "local_path": ".devcontainer/poetry_subproject/kernel.json"
            }
        ]
    },

    "postCreateCommand": "/bin/bash -c 'pwd && ls -la && bash post-create.sh'",


    "remoteUser": "jovyan"
}
